#include <stdio.h>

int main()
{
    int T,i;
    double a, s, min, max;
    scanf("%d", &T);
    while (T--) {
	min = 10.0;
	max = 0.0;
	s = 0;
	for (i = 0; i < 10; i++) {
	    scanf("%f", &a);
	    if (a < min)
		min = a;
	    if (a > max)
		max = a;
	    s += a;
	}
 
	if (min 

    }
    return 0;
}
